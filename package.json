{
  "name": "plus1-ecosystem",
  "version": "1.0.0",
  "private": true,
  "description": "Plus1 Rewards deployment",
  "scripts": {
    "dev": "cd plus1-rewards && npm run dev",
    "build": "cd plus1-rewards && npm run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "fs-extra": "^11.2.0"
  }
}
